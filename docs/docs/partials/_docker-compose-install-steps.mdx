### Start the external server

This client repository does not ship Docker Compose files or a server image. Start and deploy the sibling `photo-classifier` project by following its `README.md` and deployment documentation.

The Go server listens on `http://127.0.0.1:8080` by default. Configure the Web or Mobile client to use the deployed server URL.
